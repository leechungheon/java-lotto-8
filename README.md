# 프리코스 - 2주차 과제

### **👉입력조건**

- **로또 구입 금액**을 입력받는다.
- 1000으로 나누었을 때 나머지가 0이어야 한다.

    ```smalltalk
    구입금액을 입력해 주세요.
    14000
    ```

- **당첨 번호**를 입력받는다.
- 당첨 번호는 쉼표(,)로 구분된다.
- 당첨 번호는 1이상 45이하의 중복되지 않는 6개의 숫자이다.

    ```smalltalk
    당첨 번호를 입력해 주세요.
    1,2,3,4,5,6
    ```

- **보너스 번호**를 입력받는다.
- 당첨 번호와 중복되지 않는 1이상 45이하의 숫자가 돼야 한다.

    ```smalltalk
    보너스 번호를 입력해 주세요.
    7
    ```


### **👉기능**

- 당첨은 1등부터 5등까지 있다. 당첨 기준과 금액은 아래와 같다.
    - 1등: 6개 번호 일치 / 2,000,000,000원
    - 2등: 5개 번호 + 보너스 번호 일치 / 30,000,000원
    - 3등: 5개 번호 일치 / 1,500,000원
    - 4등: 4개 번호 일치 / 50,000원
    - 5등: 3개 번호 일치 / 5,000원
- 로또 구입 금액을 입력하면 구입 금액에 해당하는 만큼 로또를 발행해야 한다.

### **👉출력조건**

- 발행한 로또 수량 및 번호를 출력한다.
- 로또 번호는 오름차순으로 정렬하여 보여준다.

    ```smalltalk
    8개를 구매했습니다.
    [8, 21, 23, 41, 42, 43] 
    [3, 5, 11, 16, 32, 38] 
    [7, 11, 16, 35, 36, 44] 
    [1, 8, 11, 31, 41, 42] 
    [13, 14, 16, 38, 42, 45] 
    [7, 11, 30, 40, 42, 43] 
    [2, 13, 22, 32, 38, 45] 
    [1, 3, 5, 14, 22, 45]
    ```

- 당첨 내역을 출력한다.
- 수익률은 소수점 둘째 자리에서 반올림한다.

    ```smalltalk
    당첨 통계
    ---
    3개 일치 (5,000원) - 1개
    4개 일치 (50,000원) - 0개
    5개 일치 (1,500,000원) - 0개
    5개 일치, 보너스 볼 일치 (30,000,000원) - 0개
    6개 일치 (2,000,000,000원) - 0개
    총 수익률은 62.5%입니다.
    ```


## **💻프로그래밍 요구 사항**

- 함수(또는 메서드)의 길이가 15라인을 넘어가지 않도록 구현한다.
    - 함수(또는 메서드)가 한 가지 일만 잘 하도록 구현한다.
- else 예약어를 쓰지 않는다.
    - else를 쓰지 말라고 하니 switch/case로 구현하는 경우가 있는데 switch/case도 허용하지 않는다.
    - 힌트: if 조건절에서 값을 return하는 방식으로 구현하면 else를 사용하지 않아도 된다.
- Java Enum을 적용하여 프로그램을 구현한다.
- 구현한 기능에 대한 단위 테스트를 작성한다. 단, UI(System.out, System.in, Scanner) 로직은 제외한다.
    - 단위 테스트 작성이 익숙하지 않다면 `LottoTest`를 참고하여 학습한 후 테스트를 작성한다.
- 제공된 `Lotto` 클래스를 사용하여 구현해야 한다.
- `Lotto`에 `numbers` 이외의 필드(인스턴스 변수)를 추가할 수 없다.
- `numbers`의 접근 제어자인 `private`은 변경할 수 없다.
- `Lotto`의 패키지를 변경할 수 있다.

```java
public class Lotto {
    private final List<Integer> numbers;

    public Lotto(List<Integer> numbers) {
        validate(numbers);
        this.numbers = numbers;
    }

    private void validate(List<Integer> numbers) {
        if (numbers.size() != 6) {
            throw new IllegalArgumentException("[ERROR] 로또 번호는 6개여야 합니다.");
        }
    }

    // TODO: 추가 기능 구현
}
```

## 🤓기능 목록 분해

| **분류** | **핵심 기능** | **세부 기능** | **관련 요구 사항** |
| --- | --- | --- | --- |
| **입력 처리** | 1. 구입 금액 입력 | 1.1. 금액 입력 받기 | 금액은 1,000원 단위 |
|  |  | 1.2. 금액 유효성 검사 | 1,000원 단위, `IllegalArgumentException` 처리 |
|  |  | 1.3. 당첨 번호 입력 및 유효성 검사 | 1~45 범위, 6개, 중복X, 쉼표 구분 |
|  |  | 1.4. 보너스 번호 입력 및 유효성 검사 | 1~45 범위, 당첨 번호와 중복 X |
| **로또 발행** | 2. 로또 자동 생성 | 2.1. 구입 수량 계산 (금액 / 1,000) | 로또 1장 1,000원 |
|  |  | 2.2. 중복 없는 6개 번호 생성 (1~45) |  |
| **결과 비교/산출** | 3. 당첨 결과 계산 | 3.1. 구매 로또별 당첨 번호 일치 개수 확인 | 3, 4, 5, 6개 일치 여부 |
|  |  | 3.2. 5개 일치 시 보너스 번호 일치 여부 확인 (2등) |  |
|  | 3.3. 수익률 계산 | 3.3.1. 총 상금 합산 | 각 등수별 상금 |
|  |  | 3.3.2. 수익률 = (총 상금 / 구입 금액) * 100 |  |
| **출력** | 4. 결과 출력 | 4.1. 구입 수량 및 로또 번호 출력 | 로또 번호 오름차순 정렬 |
|  |  | 4.2. 당첨 통계 출력 | 등수별 당첨 개수 |
|  |  | 4.3. 수익률 출력 | 소수점 둘째 자리에서 반올림 |